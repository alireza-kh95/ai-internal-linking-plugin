WordPress now checks this repository's published releases for plugin updates.

Existing destinations in nested ACF link fields, service cards and WYSIWYG content are excluded from new suggestions. Saved suggestions are checked again when displayed and applied.

Install this version once through Plugins > Add New Plugin > Upload Plugin, choosing to replace the installed version. Do not delete the plugin first. Future versioned releases appear through WordPress's normal plugin update checks.

